import SwiftUI

extension Color {
    /// The protective accent — a desaturated indigo/violet. Drives prominence and selection.
    static let guardAccent = Color.accentColor

    /// The quit / destructive signifier.
    static let guardDanger = Color("DangerColor")

    /// The "protected / safe" signifier.
    static let guardProtected = Color("ProtectedColor")
}
